# ─────────────────────────────────────────────────────────────────────────────
# http-honeypot — environment variable reference
# Copy this file to .env and fill in your values.
# The .env file is loaded automatically by docker-compose.
# ─────────────────────────────────────────────────────────────────────────────

# ── General ──────────────────────────────────────────────────────────────────

# Human-readable name for this instance (included in all notifications)
NAME=my-honeypot

# Host port (container always uses 80)
HONEYPOT_PORT=80

# ── Tar-pit ───────────────────────────────────────────────────────────────────

# Max random delay per request in seconds. Uses crypto/rand. Set 0 to disable.
TAR_PIT_MAX_SEC=20

# ── Rate limiting ─────────────────────────────────────────────────────────────

# Max requests per IP per minute before returning 429.
# Default is very high so the honeypot collects as much data as possible.
RATE_LIMIT_PER_MIN=1000

# ── Logging ───────────────────────────────────────────────────────────────────

# Set true to disable ALL file logging.
# Pushover and webhook notifications still fire when LOG_DISABLED=true.
LOG_DISABLED=false

# Rotate /var/log/honeypot.jsonl when it exceeds this size in MB.
LOG_MAX_SIZE_MB=100

# ── Pushover ──────────────────────────────────────────────────────────────────
# https://pushover.net/

PUSHOVER_APP=
PUSHOVER_RECIPIENT=

# ISO 3166-1 alpha-2 code — alert when a request comes from this country.
# Throttled to once per hour. Example: DE  US  CN  RU
PUSHOVER_NOTIFY_COUNTRY=

# ── Webhook ───────────────────────────────────────────────────────────────────
# POST JSON to this URL on every attack event and country notification.
# Compatible with n8n, Slack incoming webhooks, Make, Zapier, etc.
# Payload includes: event, server, timestamp, ip, method, host, path,
# user_agent, is_attack, attack_tag, api_key_used, post_body, ipinfo.

WEBHOOK_URL=
WEBHOOK_SECRET=

# ── Prometheus /metrics ───────────────────────────────────────────────────────
# Protected by HTTP Basic Auth. Change both values before exposing to internet!

METRICS_USER=admin
METRICS_PASSWORD=password
METRICS_REALM=Prometheus Server

# Set true to disable the /metrics endpoint entirely.
METRICS_DISABLED=false
